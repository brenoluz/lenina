# Ralph Progress Log
Started: Wed Mar  4 09:18:46 AM CET 2026
---

## Codebase Patterns
- Python 3.10+ with FastAPI for REST API endpoints
- Pydantic models for request/response validation
- Global state management for Anvil process lifecycle
- Use `os.setsid` for process group management on Unix systems
- Use `os.killpg(os.getpgid(pid), signal.SIGTERM)` for graceful process termination

## 2026-03-04 - US-001
- Implemented POST `/anvil/start` endpoint
- Created FastAPI application with AnvilConfig and AnvilStartResponse models
- Implemented process lifecycle management with subprocess.Popen
- Added proper error handling for already running and failed start scenarios
- Created requirements.txt and pyproject.toml for project configuration

**Files changed:**
- main.py (created)
- requirements.txt (created)
- pyproject.toml (created)
- tasks/prd.json (updated with passes: true)

**Learnings for future iterations:**
- Use `os.setsid` with preexec_fn to properly manage child process groups for clean shutdown
- Store global state (anvil_process, anvil_config, anvil_start_time) at module level
- Always check `process.poll() is None` to determine if a subprocess is still running
- Wait briefly after starting Anvil (asyncio.sleep) to catch immediate startup failures
---

## 2026-03-04 - US-002
- Implemented POST `/anvil/stop` endpoint
- Created AnvilStopResponse Pydantic model
- Implemented graceful process termination using SIGTERM for process groups
- Added fallback to SIGKILL if graceful shutdown times out
- Properly clears global state after stopping (anvil_process, anvil_start_time, anvil_config)

**Files changed:**
- main.py (added stop_anvil endpoint and AnvilStopResponse model)
- tasks/prd.json (updated with passes: true)

**Learnings for future iterations:**
- Use `os.killpg(os.getpgid(pid), signal.SIGTERM)` for clean process group termination on Unix
- Always clear global state after stopping to allow clean restart
- Handle subprocess.TimeoutExpired to force kill if graceful shutdown fails
---

## 2026-03-04 09:00 - US-003
- Implemented POST `/anvil/restart` endpoint
- Created AnvilRestartResponse Pydantic model
- Endpoint stops running instance (if any) and starts with preserved or new config
- Reuses same graceful termination logic as stop endpoint
- Clears global state before restart, resets deployed contracts tracking
- Returns 200 with new PID, port, chainId, status, and message

**Files changed:**
- main.py (added AnvilRestartResponse model and restart_anvil endpoint)
- tasks/prd.json (updated with passes: true)

**Learnings for future iterations:**
- When restarting, check if process exists but isn't running to clean up stale state
- Preserve config from anvil_config global if no new config provided
- Inline the start logic rather than calling start_anvil to avoid double-checking "already running" errors
- Reset deployed_contracts on restart to match fresh blockchain state
---

## 2026-03-04 10:41 - US-004
- Implemented GET `/anvil/keys` endpoint
- Created AnvilKeysResponse and PrivateKeyInfo Pydantic models
- Implemented account parsing from Anvil's startup output using regex
- Added anvil_accounts global state to store parsed accounts
- Returns 400 if Anvil is not running, 500 if accounts couldn't be parsed
- Response includes accounts list with address and privateKey for each, plus optional mnemonic

**Files changed:**
- main.py (added AnvilKeysResponse model, get_private_keys endpoint, account parsing logic, anvil_accounts global state)
- tasks/prd.json (updated with passes: true)

**Learnings for future iterations:**
- Use `stderr=subprocess.STDOUT` to capture all Anvil output in a single stream for parsing
- Anvil outputs accounts and keys in numbered format: `(0) 0x... (10000 ETH)` and `(0) 0x...`
- Parse both addresses and private keys using regex patterns, then match by index
- Always clear anvil_accounts when stopping/restarting to avoid stale data
- Declare all modified globals in each function that modifies them
---

## 2026-03-04 - US-005
- Implemented GET `/anvil/contract/{address}` endpoint
- Created ContractDetailsResponse Pydantic model with address, bytecodeHash, bytecode fields
- Used httpx async client to make eth_getCode RPC calls to Anvil
- Validates Ethereum address format with regex before making RPC call
- Returns 404 when bytecode is "0x" (no contract), 200 with bytecode details when deployed
- Returns 400 if Anvil is not running or RPC errors occur
- Calculates SHA256 hash of bytecode for bytecodeHash field

**Files changed:**
- main.py (added ContractDetailsResponse model, get_contract endpoint, httpx import)
- tasks/prd.json (updated with passes: true)

**Learnings for future iterations:**
- Use `eth_getCode` RPC method to check contract existence - returns "0x" for non-contracts
- Validate address format with `^0x[0-9a-fA-F]{40}$` regex before making RPC calls
- Use `httpx.AsyncClient` for async HTTP requests in FastAPI endpoints
- Always wrap RPC calls in try/except for proper error handling (timeouts, connection errors)
- SHA256 hash of bytecode provides a unique identifier for contract code
---

## 2026-03-04 - US-006
- Implemented GET `/anvil/config` endpoint
- Created AnvilConfigResponse Pydantic model with ip, port, chainId, version, blockTime, gasLimit, mnemonic fields
- Returns 400 if Anvil is not running, 500 if config unavailable
- Reuses anvil_config global state populated during Anvil start
- Response includes all configuration parameters: IP (127.0.0.1), port, chainId, version, blockTime, gasLimit, mnemonic

**Files changed:**
- main.py (added AnvilConfigResponse model and get_config endpoint)
- tasks/prd.json (updated with passes: true)

**Learnings for future iterations:**
- Store all Anvil config in anvil_config global dict at module level for easy access
- Use ip="127.0.0.1" as default since Anvil binds to localhost
- Follow existing pattern: check `anvil_process is None or anvil_process.poll() is not None` for running state
- Return 500 if config is missing even when process is running (defensive programming)
---

## 2026-03-04 - US-007
- Implemented GET `/anvil/status` endpoint
- Created AnvilStatus Pydantic model (already existed, reused)
- Endpoint returns running state with pid, uptime, and port
- Returns 200 regardless of running state (returns nulls when not running)
- Reuses existing global state (anvil_process, anvil_start_time, anvil_config)

**Files changed:**
- main.py (added get_anvil_status endpoint)
- tasks/prd.json (updated with passes: true)

**Learnings for future iterations:**
- Return type annotation `-> AnvilStatus` required for strict mypy compliance
- Use `anvil_process.poll() is not None` to check if process is not running
- Calculate uptime as `time.time() - anvil_start_time` when both are available
- Always return 200 for status endpoints - the `running` field indicates state
---

